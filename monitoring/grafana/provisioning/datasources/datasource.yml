apiVersion: 1

datasources:
  - name: Loki
    uid: loki-datasource
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    jsonData:
      maxLines: 1000
    editable: false