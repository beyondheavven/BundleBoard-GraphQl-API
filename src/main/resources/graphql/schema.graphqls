type Mutation {
    """
    Служебная заглушка. GraphQL запрещает пустые типы.
    Все реальные мутации собираются через 'extend type Mutation' из других файлов.
    """
    _empty: String
}

type Query {
    debugSecurity: String!
}

type Tag {
    id: ID!
    name: String!
}