extend type Mutation {
    createCheckoutSession(input: PaymentRequest!): String
    updateStripeAccount(stripeAccountId: String!): UserProfileResponse!
}

input PaymentRequest {
    userId: ID!
    currency: String!
    collectionIds: [ID!]!
}