extend type Mutation {
    createCheckoutSession(input: PaymentRequest!): String!
}

input PaymentRequest {
    clientId: ID!
    currency: String!
    items: [CartItem!]!
}

input CartItem {
    id: ID!
    name: String!
    price: Float!
}